/**
 * useBackendSync — Fase 2 vai reescrever este hook para persistir progresso
 * em `public.progresso` / `user_saldo`. Por agora é um no-op.
 */
export function useBackendSync() {
  /* noop until Phase 2 */
}
